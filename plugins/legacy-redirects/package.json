{
  "name": "legacy-redirects",
  "version": "0.1.0",
  "private": true,
  "description": "Redirects from v4 URLs that v5's case redirects don't cover (removed commas, collapsed dashes).",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "main": "./dist/index.js",
  "quartz": {
    "name": "legacy-redirects",
    "displayName": "Legacy (v4) redirects",
    "description": "Redirects from v4 URLs that v5's case redirects don't cover (removed commas, collapsed dashes).",
    "category": "emitter",
    "version": "0.1.0",
    "quartzVersion": ">=5.0.0",
    "defaultEnabled": true,
    "defaultOptions": {}
  }
}
